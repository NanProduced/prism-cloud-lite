spring:
  application:
    name: core-service
  profiles:
    active: ${profileActive:dev}
  jackson:
    time-zone: UTC
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
