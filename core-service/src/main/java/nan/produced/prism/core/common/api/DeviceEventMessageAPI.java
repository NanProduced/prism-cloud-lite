package nan.produced.prism.core.common.api;

// 重新导出 DeviceEventMessage 作为公开 API
public class DeviceEventMessageAPI {
    // 这是一个虚拟类，用于在 common.api 包中创建命名空间
    // 实际的 DeviceEventMessage 在 common.messaging 中定义
    // 这个文件确保 Spring Modulith 认可 common.api 为公开 API 层

    private DeviceEventMessageAPI() {
        // Utility class
    }
}
